import React from'react';import{resolveFieldConfiguration}from'../../lib/fieldConfigurationResolver.js';
export default function FieldConfigurationPreview({field}){const r=resolveFieldConfiguration(field);return <section className="configuration-preview" aria-label="Projection théorique"><h4>Prévisualisation théorique seulement</h4><ol>{r.trace.map(x=><li key={x.layer}><strong>{x.layer}</strong><code>{JSON.stringify(x.input)}</code></li>)}</ol><h5>Projection finale</h5><pre>{JSON.stringify(r.projection,null,2)}</pre><p>Aucun composant métier ne consomme cette projection.</p></section>}
