import React from'react';import{diagnoseFieldConfiguration}from'../../lib/fieldConfigurationResolver.js';import'../../features/v13/field-catalog-activation.css';
export default function FieldConfigurationDiagnosticsTab({field}){const d=diagnoseFieldConfiguration(field);return <section className="configuration-readonly"><header><h3>Diagnostics des configurations</h3><strong>Lecture uniquement — aucune correction automatique</strong></header>{d.length?<ul>{d.map((x,i)=><li key={`${x.code}-${i}`}><strong>{x.code}</strong> · {x.level} · {x.message} · {x.blocking?'bloquant':'informatif'}</li>)}</ul>:<p>Aucune incohérence statique détectée.</p>}<p>Contrôles couverts : contrat/version, JSON, champ orphelin, conflit, cycle, métadonnées, relation et brouillon.</p></section>}
