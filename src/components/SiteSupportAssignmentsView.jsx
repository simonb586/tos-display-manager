import React,{useEffect,useMemo,useState}from'react';import{Download,ExternalLink,RefreshCw,Search}from'lucide-react';import GridPagination from'./GridPagination';import{getMarketingAssignmentsBySiteAndSupport,getOperationalCommunicationAssignmentsBySiteAndSupport}from'../services/siteSupportBusinessService';import{BUSINESS_CONTEXT}from'../lib/businessContext';
const columns=[['site','Site'],['support_id','Support'],['infrastructure','Infrastructure'],['client','Client'],['campaign','Campagne / communication'],['visual','Visuel'],['format','Format'],['statut','Statut'],['no_edt','EDT'],['date_completion','Installation'],['date_debut','Date début'],['date_fin','Date fin'],['derniere_activite','Dernière activité']];
const empty={search:'',site:'',support:'',client:'',campaign:'',visual:'',format:'',status:'',edt:'',dateFrom:'',dateTo:'',issue:''},csvCell=value=>`"${String(value??'').replaceAll('"','""')}"`;
const exportLabels=Object.freeze({page:'Page visible',selection:'Sélection',filtered:'Ensemble filtré'});
function exportCsv(rows,name){const content=[columns.map(([,label])=>csvCell(label)).join(','),...rows.map(row=>columns.map(([key])=>csvCell(row[key])).join(','))].join('\r\n');const url=URL.createObjectURL(new Blob(['\ufeff',content],{type:'text/csv;charset=utf-8'})),link=document.createElement('a');link.href=url;link.download=name;link.click();URL.revokeObjectURL(url)}
export default function SiteSupportAssignmentsView({context,onNavigate,role}){const operational=context===BUSINESS_CONTEXT.OPERATIONAL,[result,setResult]=useState({rows:[],total:0,page:1,pageSize:25}),[filters,setFilters]=useState(empty),[applied,setApplied]=useState(empty),[selected,setSelected]=useState(new Set()),[error,setError]=useState(''),[loading,setLoading]=useState(true);const canEdit=['Administrateur','Coordonnateur'].includes(role);
 async function load(page=1,pageSize=result.pageSize,nextFilters=applied){setLoading(true);setError('');try{const loader=operational?getOperationalCommunicationAssignmentsBySiteAndSupport:getMarketingAssignmentsBySiteAndSupport;setResult(await loader({page,pageSize,filters:nextFilters}));setSelected(new Set())}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load(1,25,empty)},[context]);
 const visible=useMemo(()=>{const term=applied.search.trim().toLowerCase();return term?result.rows.filter(row=>Object.values(row).some(value=>String(value??'').toLowerCase().includes(term))):result.rows},[result.rows,applied.search]),picked=visible.filter(row=>selected.has(row.logical_key)),title=operational?'Communications opérationnelles par site et supports':'Campagnes et visuels par site et supports';
 const go=(target,row,action)=>{sessionStorage.setItem('tos_assignment_context',JSON.stringify({action,support_id:row.support_id,campaign_id:row.campaign_id,visual_id:row.visual_id,edt:row.no_edt,business_context:context}));onNavigate?.(target,row)};
 const fields=[['site','Site'],['support','Support'],['client','Client'],['campaign',operational?'Communication':'Campagne'],['visual','Visuel'],['format','Format'],['status','Statut'],['edt','EDT'],['issue','Enjeu']];
 return <div className="tablePage assignment-page"><header className="assignment-header"><div><span className="eyebrow">Projection métier unique</span><h1>{title}</h1><p>Clé : site + support + campagne/communication + visuel.</p></div><button onClick={()=>load(result.page,result.pageSize)}><RefreshCw/> Actualiser</button></header><form className="assignment-toolbar" onSubmit={e=>{e.preventDefault();setApplied(filters);load(1,result.pageSize,filters)}}><label><Search/><input value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})} placeholder="Recherche"/></label>{fields.map(([key,label])=><input key={key} aria-label={label} placeholder={label} value={filters[key]} onChange={e=>setFilters({...filters,[key]:e.target.value})}/>)}<input aria-label="Période début" type="date" value={filters.dateFrom} onChange={e=>setFilters({...filters,dateFrom:e.target.value})}/><input aria-label="Période fin" type="date" value={filters.dateTo} onChange={e=>setFilters({...filters,dateTo:e.target.value})}/><button type="submit">Filtrer</button><button type="button" onClick={()=>{setFilters(empty);setApplied(empty);load(1,result.pageSize,empty)}}>Réinitialiser</button><button type="button" onClick={()=>exportCsv(visible,'affectations-page-visible.csv')}><Download/> Page</button><button type="button" disabled={!picked.length} onClick={()=>exportCsv(picked,'affectations-selection.csv')}><Download/> Sélection</button></form>{error&&<div className="sync-error-banner">{error}</div>}{loading?<div className="client-loading">Chargement…</div>:<div className="client-table-wrap"><table><thead><tr><th><input aria-label="Sélectionner la page" type="checkbox" checked={visible.length>0&&picked.length===visible.length} onChange={e=>setSelected(e.target.checked?new Set(visible.map(row=>row.logical_key)):new Set())}/></th>{columns.map(([,label])=><th key={label}>{label}</th>)}<th>Actions</th></tr></thead><tbody>{visible.map(row=><tr key={row.logical_key}><td><input aria-label={`Sélectionner ${row.support_id}`} type="checkbox" checked={selected.has(row.logical_key)} onChange={()=>setSelected(current=>{const next=new Set(current);next.has(row.logical_key)?next.delete(row.logical_key):next.add(row.logical_key);return next})}/></td>{columns.map(([key])=><td key={key}>{String(row[key]??'—')}</td>)}<td className="assignment-actions"><button onClick={()=>go(operational?'Communications opérationnelles':'Campagnes maîtres',row,canEdit?'edit':'open')}><ExternalLink/> {canEdit?'Modifier':'Ouvrir'}</button><button onClick={()=>go(operational?'Communication opérationnelle — Visuels':'Campagne — Visuels et formats',row,'visual')}>Visuel</button><button onClick={()=>go('Infrastructures',row,'support360')}>Support / Fiche 360</button>{row.no_edt&&<button onClick={()=>go('Centre EDT et BT',row,'edt')}>EDT</button>}<button onClick={()=>go('Photos et inventaire',row,'photos')}>Photos</button><button onClick={()=>go('Édition — Historique',row,'history')}>Historique</button></td></tr>)}</tbody></table></div>}<GridPagination page={result.page} pageCount={Math.max(1,Math.ceil(result.total/result.pageSize))} pageSize={result.pageSize} total={result.total} selectedCount={selected.size} onPage={page=>load(page,result.pageSize)} onPageSize={size=>load(1,size)}/></div>}
