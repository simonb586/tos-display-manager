export const ACTIVITY_CONFIDENCE=new Set(['exact','derived']);
export function activityDedupKey(event){if(event.sourceSystem&&event.sourceRecordId!=null)return`${event.sourceSystem}:${event.sourceRecordId}`;const strict=[event.occurredAt,event.actorId||event.actorEmail,event.action,event.entityType,event.entityId];return strict.every(Boolean)?strict.join('|'):null;}
export function normalizeHistoricalEvent(candidate){if(!candidate||!ACTIVITY_CONFIDENCE.has(candidate.confidence))return null;if(!candidate.occurredAt||!candidate.action||!candidate.sourceSystem)return null;const key=activityDedupKey(candidate);if(!key)return null;return{...candidate,dedupKey:key,reconstructionMethod:candidate.confidence==='exact'?'direct':'derived',actorId:candidate.actorId||null,actorEmail:candidate.actorEmail||null};}
export function reconstructHistoricalEvents(candidates){const unique=new Map();for(const candidate of candidates||[]){const event=normalizeHistoricalEvent(candidate);if(event&&!unique.has(event.dedupKey))unique.set(event.dedupKey,event);}return[...unique.values()];}
