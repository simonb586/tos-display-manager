import{supabase,supabaseConfigured}from'../lib/supabaseClient.js';import{normalizeTerrainConfig,TERRAIN_CONFIG_VERSION}from'../lib/fieldCatalogTerrainDraft.js';
export async function saveFieldTerrainDraft({field,draft,expectedUpdatedAt,role}){if(role!=='Administrateur')throw new Error('Permission administrateur requise.');const check=normalizeTerrainConfig(draft,field);if(!check.valid){const e=new Error(Object.values(check.errors)[0]);e.validationErrors=check.errors;throw e;}if(!supabaseConfigured||!supabase)throw new Error('Supabase n’est pas configuré.');const{data,error}=await supabase.rpc('save_relation_field_terrain_draft_v0131a7',{p_table_name:field.tableName,p_field_name:field.technicalName,p_contract_version:TERRAIN_CONFIG_VERSION,p_terrain_config:check.normalized,p_expected_updated_at:expectedUpdatedAt});if(error){try{const d=JSON.parse(error.details||'{}');error.code=d.code||error.code;error.field=d.field;}catch{}throw error;}return data;}
