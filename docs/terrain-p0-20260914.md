Terrain — Inspection, installation et retrait facultatif

L'installateur choisit un visuel compatible avec le support, sans sélectionner un EDT. L'EDT éventuel vient du visuel. L'administrateur peut gérer ce rattachement depuis la fiche Visuel et depuis la fiche EDT ; les deux utilisent `campagne_visuels_formats.edt_phase_id`. Les affectations supplémentaires de supports aux EDT restent administratives et ne sont jamais créées automatiquement par une installation.

Une nouvelle installation remplace la photo principale et conserve les précédentes dans la galerie, avec le libellé « Archivée ». Aucun retrait séparé n'est requis lors d'un remplacement. L'action facultative « Retrait — support sans affiche » conserve les photos et l'historique, enregistre une photo du support après retrait et vide les références du visuel actuellement installé. Une réinstallation ultérieure redéfinit une photo principale.

L'échec Inspection provenait d'un répertoire Storage normalisé différemment du `support_id` canonique, puis d'un contexte RPC non initialisé sans EDT et de métadonnées photo client/bucket manquantes. Le chemin conserve désormais l'identifiant canonique exact. Les buckets restent privés et les policies RLS ne sont pas élargies.

Ordre des migrations, déjà validées localement puis appliquées au projet `cmdfomowtzrinywdsosy` :

1. `20260914102208_terrain_inspection_optional_context_p0.sql`
2. `20260914103135_terrain_visual_driven_installation_p0.sql`
3. `20260914103142_admin_visual_edt_assignment_p0.sql`
4. `20260914103706_terrain_private_photo_metadata_p0.sql`
5. `20260914110911_terrain_visual_optional_phase_owner_p0.sql`
6. `20260914112600_terrain_optional_removal_and_replacement_p0.sql`

La finalisation Installation utilise `finaliser_installation_terrain_v1343`. Inspection, Enjeu, Autre photo et Retrait réutilisent le point d'entrée canonique `finaliser_intervention_terrain_v1342`. Le writer historique interne reste inaccessible directement aux clients non autorisés. Les contrôles serveur vérifient le rôle canonique, le client, le support, le visuel et l'objet Storage privé détenu par l'acteur. Les écritures de retrait sont atomiques et idempotentes ; aucune relation ou table métier en doublon n'est ajoutée.

Validation : PostgreSQL local avec les véritables triggers photo ; parcours mobiles Auth/RPC/Storage réels sur des fixtures isolées ; deux installations successives, inspection, retrait, reprise après refresh et réinstallation ; refus Client B et rôle Client ; galerie réelle avec les métadonnées des essais. HA-87719 -02 : 21 visuels en base/RPC/UI. VH-VAUD-16 : 42, visuel 34 prioritaire, 89/90 exclus. Tests globaux, Auth 24 h/Safe Links, `npm run check` et build font partie du contrôle de livraison.

Les tests historiques adaptés dans ce correctif suivent les composants et lecteurs sécurisés actuellement utilisés : résumé du dashboard, BusinessTable, CampaignsPanel, aperçu avec profil cible explicite, dialogue Visuel et registres extraits de main.jsx. Ils ne réintroduisent pas les anciens lecteurs pour satisfaire des assertions obsolètes.

Aucun changement de configuration Auth ni nouvelle variable de production. Les avertissements existants concernant la taille des bundles et les imports photo statiques/dynamiques restent présents.
