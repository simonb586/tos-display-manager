import assert from'node:assert/strict';import fs from'node:fs';const s=fs.readFileSync('supabase/V1_3_6_4_EXO_CANONICAL_OWNERSHIP_AND_CLIENT_PORTAL_FINAL_PREPARED.sql','utf8');assert.match(s,/v_exo bigint:=2/);assert.match(s,/V1364_CANONICAL_EXO_ID2_MISMATCH/);assert.match(s,/update public\.clients set nom_client='EXO' where id=v_exo/);assert.doesNotMatch(s,/delete from public\.clients/i);assert.doesNotMatch(s,/unaccent\s*\(/i);console.log('V1.3.6.4 EXO canonical ID 2 PASS.');
