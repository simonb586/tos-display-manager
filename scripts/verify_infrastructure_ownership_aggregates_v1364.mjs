import assert from'node:assert/strict';import fs from'node:fs';const s=fs.readFileSync('supabase/VERIFIER_V1_3_6_4_EXO_CANONICAL_OWNERSHIP_READ_ONLY.sql','utf8');assert.match(s,/sum\(total\)filter\(where client_id is null\) without_client/);assert.match(s,/to_jsonb\(i\)->>'client_id'/);const synthetic=Array.from({length:6619},()=>null);assert.equal(synthetic.filter(x=>x===null).length,6619);assert.doesNotMatch(s,/infrastructures.*6619/is);console.log('V1.3.6.4 infrastructure NULL aggregates PASS.');
