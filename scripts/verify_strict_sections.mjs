import fs from 'node:fs';import assert from 'node:assert/strict';import {offlineBrowser} from './lib/offlineBrowser.mjs';const records=[];await offlineBrowser('scripts/fixtures/strict-sections-entry.jsx',async({evaluate,waitFor,sleep,exceptions,consoleErrors})=>{for(const [kind,selector] of [['command','.command-tabs'],['list','.field-catalog-filters'],['drawer','.field-catalog-tabs']]){await evaluate('mount('+JSON.stringify(kind)+')');await sleep(80);const count=await evaluate('document.querySelectorAll('+JSON.stringify(selector+' button')+').length');for(let i=0;i<count;i++){await evaluate('document.querySelectorAll('+JSON.stringify(selector+' button')+')['+i+'].click()');await sleep(30);assert.equal(await evaluate('[...document.querySelectorAll('+JSON.stringify(selector+' button')+')].indexOf(document.querySelector('+JSON.stringify(selector+' .active')+'))'),i);records.push({kind,index:i,result:'PASS_LOCAL',tests:'all section buttons update active section'})}if(kind==='drawer'){assert.equal(await evaluate('[...document.querySelectorAll("button")].find(n=>n.textContent.trim()==="Activer").disabled'),true)}}assert.deepEqual(exceptions,[]);assert.deepEqual(consoleErrors,[]);fs.writeFileSync('docs/stabilization-local/certification/strict/sections-results.json',JSON.stringify({records,exceptions,consoleErrors},null,2));});console.log(records.length+' strict sections PASS');