import assert from'node:assert/strict';import fs from'node:fs';const s=fs.readFileSync('supabase/V1_3_6_4_EXO_CANONICAL_OWNERSHIP_AND_CLIENT_PORTAL_FINAL_PREPARED.sql','utf8');assert.match(s,/set statut='Désactivé' where id=1/);assert.match(s,/legacy_client_id/);assert.doesNotMatch(s,/nom_client='EXO' where id=1/);assert.doesNotMatch(s,/delete from public\.clients/i);console.log('V1.3.6.4 Client demo legacy PASS.');
