import React from 'react';import {createRoot} from 'react-dom/client';import Support from '../../src/components/Support360Panel';import Review from '../../src/components/PhotoReviewQueue';import Diagnostics from '../../src/components/TerrainSyncDiagnostics';import Recent from '../../src/components/RecentActivityWidget';
window.fixture={calls:[]};window.testApi=(file,name,args)=>{if(name==='formatTerrainSyncDate')return '';return api(file,name,args)};async function api(file,name,args){fixture.calls.push({name,args});if(name==='getSignedPhotoUrl')return 'data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///ywAAAAAAQABAAACAUwAOw==';if(name==='loadSupport360'){const rows=[{id:1,value:'B'},{id:2,value:'A'}];return {history:rows,issues:rows,inspections:rows,workOrders:rows,edtLinks:rows,logs:rows}}if(name==='listSupportPhotos')return [];if(name==='listPhotoReviewQueue')return [{id:1,original_filename:'Test',storage_bucket:'support-photos',storage_path:'review/fixture/test.jpg',signed_thumbnail_url:'data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///ywAAAAAAQABAAACAUwAOw==',uploaded_at:'2026-09-08',review_status:'needs_review'}];if(name==='listTerrainDiagnostics')return {rows:[],total:0};if(name==='loadTerrainDiagnosticSummary')return {};if(name==='formatTerrainSyncDate')return '';if(name==='listRecentBusinessActivity')return [];throw Error(name)};const root=createRoot(document.getElementById('root'));let key=0;window.mount=(kind,role='Administrateur')=>{fixture.calls=[];const components={support:<Support supportId="SUP-2" role={role}/>,review:<Review/>,diagnostics:<Diagnostics role={role}/>,recent:<Recent role={role} onNavigate={target=>fixture.calls.push({target})}/>};root.render(<React.Fragment key={++key}>{components[kind]}</React.Fragment>)};
