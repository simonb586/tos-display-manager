import React from 'react';import {createRoot} from 'react-dom/client';import Legacy from '../../src/components/LegacyPhotoImporter';import Mass from '../../src/components/MassPhotoImporter';window.fixture={calls:[],files:[],fail:false};const url=URL.createObjectURL.bind(URL);URL.createObjectURL=b=>{fixture.files.push(b);return url(b)};HTMLAnchorElement.prototype.click=function(){};window.testApi=(file,name,args)=>{if(name==='detectLegacyPhotoMetadata')return {supportId:'SUP-2',date:'2026-09-08',confidence:'manual'};if(name==='runControlledQueue')return Promise.resolve({control:{pause(){},resume(){},cancel(){}},run:(async()=>{for(let i=0;i<args[0].length;i++)await args[1](args[0][i],i)})()});fixture.calls.push({name,args});const fail=fixture.fail;return new Promise((resolve,reject)=>setTimeout(()=>{if(fail&&['importLegacyPhoto','uploadPhotoItem'].includes(name))return reject(Error('IMPORT_ERROR'));resolve(name==='analyzePhotoItem'?{...args[0],status:'ready'}:name==='uploadPhotoItem'?{...args[0],status:'completed',resultId:1}:{ok:true})},120))};const root=createRoot(document.getElementById('root'));let key=0;window.mount=(kind,role='Administrateur')=>{fixture={calls:[],files:[],fail:false};root.render(kind==='legacy'?<Legacy key={++key} dataStore={{Infrastructures:{rows:[{support_id:'SUP-2'}]}}} session={{user:{email:'local@example.test'}}}/>:<Mass key={++key} role={role}/>)};