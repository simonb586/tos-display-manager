import React,{useState} from 'react';import {createRoot} from 'react-dom/client';import Drawer from '../../src/components/field-catalog/FieldCatalogDrawer';import List from '../../src/components/field-catalog/FieldCatalogList';import Command from '../../src/components/OperationalCommandCenter';window.testApi=()=>Promise.resolve({});const field={fieldId:'infrastructures.notes',tableName:'infrastructures',technicalName:'notes',label:'Notes',field_type:'short_text',updated_at:'2026-09-08T00:00:00Z',physical:{dataType:'text',nullable:true}};function Host({kind}){const [filter,setFilter]=useState('all');return kind==='drawer'?<Drawer field={field} role="Administrateur" catalogFields={[field]} onSaved={()=>{}} onClose={()=>{}}/>:kind==='list'?<List fields={[field]} tables={['infrastructures']} table="" query="" filter={filter} onFilterChange={setFilter}/>:<Command dataStore={{}}/>}const root=createRoot(document.getElementById('root'));let key=0;window.mount=kind=>root.render(<Host key={++key} kind={kind}/>);